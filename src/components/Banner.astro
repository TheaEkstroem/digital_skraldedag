---
const { text = "sæt kryds i kalenderen", titel = "Inviter en ven" } = Astro.props;
// const { text2 = "Inviter en ven" } = Astro.props;
---
<div class="border-t-2 border-b-2 border-[#1e1c1c] py-2 overflow-hidden uppercase">
  <div class="scrolling-text whitespace-nowrap">
    <span class="px-4">
      {text}
    </span>
    <span class="px-4">
      {text}
    </span>
    <span class="px-4">
      {text}
    </span>
    <span class="px-4">
      {text}
    </span>
    <span class="px-4">
      {text}
    </span>
  </div>
</div>

<div class="border-t-2 border-b-2 border-[#1e1c1c] py-2 overflow-hidden uppercase">
    <div class="scrolling-text whitespace-nowrap">
      <span class="px-4">
        {titel}
      </span>
      <span class="px-4">
        {titel}
      </span>
      <span class="px-4">
        {titel}
      </span>
      <span class="px-4">
        {titel}
      </span>
      <span class="px-4">
        {titel}
      </span>
      <span class="px-4">
        {titel}
      </span>
    </div>
  </div>

<style>
    @keyframes scroll {
      0% {
        transform: translateX(100%);
      }
      100% {
        transform: translateX(-100%);
      }
    }
  
    .scrolling-text {
      display: flex;
      align-items: center;
      animation: scroll 30s linear infinite;
    }
  </style>